# Password-Generator
A simple Python-based password generator that creates secure random passwords. It evaluates password strength based on length and character diversity (uppercase, lowercase, digits, symbols). The generated password can be easily copied to the clipboard for quick use.
